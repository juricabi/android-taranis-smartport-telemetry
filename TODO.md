# Nothing open

Everything raised is shipped. The most recent work on main:

- The paused-replay camera pass — first frame, 2D↔3D switch, chase/follow
  re-centre (paused and after a drag), the cold-start crash guard, the
  engage toast (5130ea52).
- The Velebit-start "wall" — damaged terrarium elevation data; a void rim is
  filled from the whole coarser ground now, not averaged into a cliff
  (d523d5ad).

New items go here as they come up.
