<resources>

    <!-- Dark counterpart of AppTheme, picked automatically in dark mode. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/toolbarBackground</item>
        <item name="colorPrimaryDark">@color/toolbarBackground</item>
        <item name="colorAccent">@color/settingsAccent</item>
        <item name="colorControlActivated">@color/settingsAccent</item>
        <item name="colorControlNormal">@color/settingsTextSecondary</item>
        <item name="android:windowBackground">@color/settingsBackground</item>
        <item name="android:colorBackground">@color/settingsBackground</item>
        <item name="android:textColorPrimary">@color/settings_text_primary</item>
        <item name="android:textColorSecondary">@color/settings_text_secondary</item>
        <item name="android:disabledAlpha">0.35</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <!-- Toolbar contents are light, because the bar itself is dark grey. -->
    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.Dark"/>

</resources>
