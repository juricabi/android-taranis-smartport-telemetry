<resources>

    <!-- Dark counterpart of AppTheme. Android picks this automatically when the
         phone is in dark mode, so the settings and sensor screens follow it. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/settingsBackground</item>
        <item name="android:colorBackground">@color/settingsBackground</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

</resources>
